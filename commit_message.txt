🧹 [code health improvement] Remove unused StoreService import in WishlistModule

🎯 What:
Removed the unused `StoreService` import from `src/components/WishlistModule.tsx`.

💡 Why:
Unused imports unnecessarily clutter the file, increase parsing time slightly, and trigger linter warnings. Removing them improves code maintainability and readability.

✅ Verification:
1. Verified file changes to ensure only the import was removed.
2. Ran `npx eslint src/components/WishlistModule.tsx` and confirmed the `@typescript-eslint/no-unused-vars` warning for `StoreService` is gone.
3. Ran the full test suite (`npx playwright test`) to ensure no regressions were introduced. All tests passed.

✨ Result:
Cleaner code, fewer lint warnings, and a marginally reduced bundle size/parsing time.
