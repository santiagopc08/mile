I have implemented the requested changes:
1. Created `blood_pressure` table in Supabase.
2. Moved "History" to "El Refugio" as a new tab. Standalone historia page deleted.
3. Created "Salud" tab. Moved `BiometricVault` and `FiscalAuditor` to this new tab.
4. Implemented `BloodPressureTracker` in the Salud tab with Systolic, Diastolic, Heart Rate, and Position.
5. Fixed Tasks and Objectives persistence. Objectives are now stored in Supabase and synced alongside Tasks.
6. Updated Navigation to replace "Historia" with "Salud".

Notes:
- Frontend verification (Playwright) was skipped due to the application requiring Supabase environment variables to boot, which are not available in this sandbox.
- TypeScript build (`npm run build`) was successful (verified type safety).
