import { Timeline } from './src/components/Timeline';
import React from 'react';

function getHandlers(component: any) {
   // A bit complex. Let's see if we can find onSubmit handlers in the tree.
}
